{{- if eq (.Values._orion_ingress_provider | default "envoy-gateway") "nginx" }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}
  labels:
    kuiper.juno-innovations.com/kuiper-instance: "{{ .Release.Name }}"
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/auth-url: "http://hubble.{{ .Release.Namespace }}.svc.cluster.local:3000/api/auth-workstation/{{ .Values.name }}/"
    nginx.ingress.kubernetes.io/large-client-header-buffers: "4 16k"
    nginx.ingress.kubernetes.io/proxy-buffer-size: "16k"
spec:
  rules:
    - host: {{ .Values.host }}
      http:
        paths:
          - path: "/gitea/{{ .Values.name }}/"
            pathType: Prefix
            backend:
              service:
                name: {{ .Release.Name }}
                port:
                  number: 8080
{{- else }}
# Envoy Gateway. HTTPRoute (path -> workstation svc) + SecurityPolicy whose
# extAuth points at hubble's auth-workstation endpoint via an EG Backend(FQDN)
# local to this namespace (the cross-ns FQDN the nginx auth-url used).
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ .Release.Name }}
  labels:
    kuiper.juno-innovations.com/kuiper-instance: "{{ .Release.Name }}"
spec:
  parentRefs:
    - name: {{ .Values._orion_gateway_name | default "juno" }}
      namespace: {{ .Values._orion_gateway_namespace | default "envoy-gateway-system" }}
  hostnames:
    - {{ .Values.host }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: "/gitea/{{ .Values.name }}/"
      backendRefs:
        - name: {{ .Release.Name }}
          port: 8080
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: Backend
metadata:
  name: {{ .Release.Name }}-auth-backend
spec:
  endpoints:
    - fqdn:
        hostname: "hubble.{{ .Release.Namespace }}.svc.cluster.local"
        port: 3000
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: SecurityPolicy
metadata:
  name: {{ .Release.Name }}-auth-workstation
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: {{ .Release.Name }}
  extAuth:
    failOpen: false
    headersToExtAuth:
      - Cookie
    http:
      backendRefs:
        - group: gateway.envoyproxy.io
          kind: Backend
          name: {{ .Release.Name }}-auth-backend
      pathOverride: "/api/auth-workstation/{{ .Values.name }}/"
{{- end }}
